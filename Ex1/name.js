function printCC(job, location, name, number) {
    return `You will work as ${job} in ${location} and marry ` + name + ` and have ` + number + ` children`;
}

console.log(printCC("Hacker", `Vienna`, `Alex`, `3`));